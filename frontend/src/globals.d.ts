/** Replaced at build time by Vite's `define` with the version from
 *  package.json — see vite.config.ts. The desktop shell reports its own
 *  version over `get_desktop_info`; this is the value shown until it does. */
declare const __APP_VERSION__: string
